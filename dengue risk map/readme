pip install dash
Download all the files and save into the same directory folder
Download the "assets" folder into this same directory - keep as a separate folder
Open app.py and run - it should open in chrome

Materials:
(i) grids.geojson - shapefile for grid polygons
(ii) maptoken.txt - you need this to plot the chloropleth map
(iii) requirements.txt - list of .py libraries required to be installed
